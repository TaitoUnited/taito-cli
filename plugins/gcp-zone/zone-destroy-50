#!/bin/bash -e
# shellcheck source=lib/all.bash
. "${taito_plugin_path:?}/lib/all.bash"

# TODO: move to Terraform module
taito::confirm "Destroy gcp build notifications" || \
  taito::skip_to_next "${@}"

gcp-zone::teardown_cloud_build_slack_notifications
taito::call_next "${@}"
