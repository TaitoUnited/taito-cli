#!/bin/bash -e

name=${1}

if taito::confirm_execution "aws" "${name}" \
  "Configure Docker Hub credentials for CI/CD pipeline"
then
  echo "TODO: instructions"
  echo
  echo "Press enter when done."
  read -r
fi

taito::call_next "${@}"
