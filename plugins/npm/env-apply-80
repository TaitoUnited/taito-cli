#!/bin/bash -e
# shellcheck source=lib/all.bash
. "${taito_plugin_path:?}/lib/all.bash"

[[ "${taito_env:-}" == "local" ]] ||
  echo "Skipping install for non-local environment" && taito::skip_to_next

npm::install "${@}"
taito::call_next "${@}"
