#!/bin/bash

if [[ "${taito_env:-}" == "local" ]]; then
  "${taito_plugin_path:?}/util/install" "${@}"
else
  echo "Skipping install for non-local environment"
fi

"${taito_util_path:?}/call-next" "${@}"
