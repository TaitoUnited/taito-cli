---
import Root from '../layouts/Root.astro';
import Page from '../components/Page.astro';
import Spacing from '../components/Spacing.astro';
import GitHubEditLink from '../components/GitHubEditLink.astro';

interface Props {
  title?: string;
  editPath: string;
}

const { title, editPath } = Astro.props;
---

<Root title={title}>
  <Page>
    <div
      data-pagefind-body
      data-pagefind-meta={title ? 'title[data-title]' : undefined}
      data-title={title}
    >
      <slot />
    </div>

    <Spacing dir="y" amount={20} />

    <GitHubEditLink path={editPath} />
  </Page>
</Root>
